/*
 * This java source file is generated by DAO4J v1.19
 * Generated on Tue Mar 06 22:47:33 CST 2018
 * For more information, please contact b-i-d@163.com
 * Please check http://sourceforge.net/projects/dao4j/ for the latest version.
 */

package modelo;

/*
 * For Table RecordDeVentas
 */
public class Recorddeventas implements java.io.Serializable, Cloneable {
    private RecorddeventasKey _key = new RecorddeventasKey();

    /* nombreTienda, PK */
    protected String nombretienda;

    /* idVenta, PK */
    protected int idventa;

    /* anio, PK */
    protected int anio;

    /* trimestre, PK */
    protected int trimestre;

    /* monto */
    protected double monto;

    /* Return the key object. */
    public RecorddeventasKey getKeyObject() {
        return _key;
    }

    /* nombreTienda, PK */
    public String getNombretienda() {
        return nombretienda;
    }

    /* nombreTienda, PK */
    public void setNombretienda(String nombretienda) {
        this.nombretienda = nombretienda;
        _key.setNombretienda(nombretienda);
    }

    /* idVenta, PK */
    public int getIdventa() {
        return idventa;
    }

    /* idVenta, PK */
    public void setIdventa(int idventa) {
        this.idventa = idventa;
        _key.setIdventa(idventa);
    }

    /* anio, PK */
    public int getAnio() {
        return anio;
    }

    /* anio, PK */
    public void setAnio(int anio) {
        this.anio = anio;
        _key.setAnio(anio);
    }

    /* trimestre, PK */
    public int getTrimestre() {
        return trimestre;
    }

    /* trimestre, PK */
    public void setTrimestre(int trimestre) {
        this.trimestre = trimestre;
        _key.setTrimestre(trimestre);
    }

    /* monto */
    public double getMonto() {
        return monto;
    }

    /* monto */
    public void setMonto(double monto) {
        this.monto = monto;
    }

    /* Indicates whether some other object is "equal to" this one. */
    public boolean equals(Object obj) {
        if (this == obj)
            return true;

        if (obj == null || !(obj instanceof Recorddeventas))
            return false;

        Recorddeventas bean = (Recorddeventas) obj;

        if (this.nombretienda == null) {
            if (bean.nombretienda != null)
                return false;
        }
        else if (!this.nombretienda.equals(bean.nombretienda)) 
            return false;

        if (this.idventa != bean.idventa)
            return false;

        if (this.anio != bean.anio)
            return false;

        if (this.trimestre != bean.trimestre)
            return false;

        if (this.monto != bean.monto)
            return false;

        return true;
    }

    /* Creates and returns a copy of this object. */
    public Object clone()
    {
        Recorddeventas bean = new Recorddeventas();
        bean.nombretienda = this.nombretienda;
        bean.idventa = this.idventa;
        bean.anio = this.anio;
        bean.trimestre = this.trimestre;
        bean.monto = this.monto;
        return bean;
    }

    /* Returns a string representation of the object. */
    public String toString() {
        String sep = "\r\n";
        StringBuffer sb = new StringBuffer();
        sb.append(this.getClass().getName()).append(sep);
        sb.append("[").append("nombretienda").append(" = ").append(nombretienda).append("]").append(sep);
        sb.append("[").append("idventa").append(" = ").append(idventa).append("]").append(sep);
        sb.append("[").append("anio").append(" = ").append(anio).append("]").append(sep);
        sb.append("[").append("trimestre").append(" = ").append(trimestre).append("]").append(sep);
        sb.append("[").append("monto").append(" = ").append(monto).append("]").append(sep);
        return sb.toString();
    }
}