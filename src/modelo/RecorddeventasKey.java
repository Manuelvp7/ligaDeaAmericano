/*
 * This java source file is generated by DAO4J v1.19
 * Generated on Tue Mar 06 22:47:33 CST 2018
 * For more information, please contact b-i-d@163.com
 * Please check http://sourceforge.net/projects/dao4j/ for the latest version.
 */

package modelo;

public class RecorddeventasKey implements java.io.Serializable, Cloneable {
    /* nombreTienda */
    protected String nombretienda;

    /* idVenta */
    protected int idventa;

    /* anio */
    protected int anio;

    /* trimestre */
    protected int trimestre;

    /* nombreTienda */
    public String getNombretienda() {
        return nombretienda;
    }

    /* nombreTienda */
    public void setNombretienda(String nombretienda) {
        this.nombretienda = nombretienda;
    }

    /* idVenta */
    public int getIdventa() {
        return idventa;
    }

    /* idVenta */
    public void setIdventa(int idventa) {
        this.idventa = idventa;
    }

    /* anio */
    public int getAnio() {
        return anio;
    }

    /* anio */
    public void setAnio(int anio) {
        this.anio = anio;
    }

    /* trimestre */
    public int getTrimestre() {
        return trimestre;
    }

    /* trimestre */
    public void setTrimestre(int trimestre) {
        this.trimestre = trimestre;
    }

    /* Calculate hash code */
    public int hashCode() {
        int hashCode = 0;
        if (nombretienda != null)  
            hashCode += nombretienda.hashCode();
        hashCode += new Integer(idventa).hashCode();
        hashCode += new Integer(anio).hashCode();
        hashCode += new Integer(trimestre).hashCode();
        return hashCode;
    }
    
    /* Indicates whether some other object is "equal to" this one. */
    public boolean equals(Object obj) {
        if (this == obj)
            return true;

        if (obj == null || !(obj instanceof RecorddeventasKey))
            return false;

        RecorddeventasKey key = (RecorddeventasKey) obj;

        if (this.nombretienda == null) {
            if (key.nombretienda != null)
                return false;
        }
        else if (!this.nombretienda.equals(key.nombretienda)) 
            return false;

        if (this.idventa != key.idventa)
            return false;

        if (this.anio != key.anio)
            return false;

        if (this.trimestre != key.trimestre)
            return false;

        return true;
    }

    /* Creates and returns a copy of this object. */
    public Object clone()
    {
        RecorddeventasKey key = new RecorddeventasKey();
        key.nombretienda = this.nombretienda;
        key.idventa = this.idventa;
        key.anio = this.anio;
        key.trimestre = this.trimestre;
        return key;
    }

    /* Returns a string representation of the object. */
    public String toString() {
        String sep = "\r\n";
        StringBuffer sb = new StringBuffer();
        sb.append(this.getClass().getName()).append(sep);
        sb.append("[").append("nombretienda").append(" = ").append(nombretienda).append("]");
        sb.append("[").append("idventa").append(" = ").append(idventa).append("]");
        sb.append("[").append("anio").append(" = ").append(anio).append("]");
        sb.append("[").append("trimestre").append(" = ").append(trimestre).append("]");
        return sb.toString();
    }
}