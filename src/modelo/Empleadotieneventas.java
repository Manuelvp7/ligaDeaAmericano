/*
 * This java source file is generated by DAO4J v1.19
 * Generated on Tue Mar 06 22:47:33 CST 2018
 * For more information, please contact b-i-d@163.com
 * Please check http://sourceforge.net/projects/dao4j/ for the latest version.
 */

package modelo;

/*
 * For Table EmpleadoTieneVentas
 */
public class Empleadotieneventas implements java.io.Serializable, Cloneable {
    private EmpleadotieneventasKey _key = new EmpleadotieneventasKey();

    /* vendedorCURP, PK */
    protected String vendedorcurp;

    /* idVenta, PK */
    protected int idventa;

    /* anio, PK */
    protected int anio;

    /* trimestre, PK */
    protected int trimestre;

    /* Return the key object. */
    public EmpleadotieneventasKey getKeyObject() {
        return _key;
    }

    /* vendedorCURP, PK */
    public String getVendedorcurp() {
        return vendedorcurp;
    }

    /* vendedorCURP, PK */
    public void setVendedorcurp(String vendedorcurp) {
        this.vendedorcurp = vendedorcurp;
        _key.setVendedorcurp(vendedorcurp);
    }

    /* idVenta, PK */
    public int getIdventa() {
        return idventa;
    }

    /* idVenta, PK */
    public void setIdventa(int idventa) {
        this.idventa = idventa;
        _key.setIdventa(idventa);
    }

    /* anio, PK */
    public int getAnio() {
        return anio;
    }

    /* anio, PK */
    public void setAnio(int anio) {
        this.anio = anio;
        _key.setAnio(anio);
    }

    /* trimestre, PK */
    public int getTrimestre() {
        return trimestre;
    }

    /* trimestre, PK */
    public void setTrimestre(int trimestre) {
        this.trimestre = trimestre;
        _key.setTrimestre(trimestre);
    }

    /* Indicates whether some other object is "equal to" this one. */
    public boolean equals(Object obj) {
        if (this == obj)
            return true;

        if (obj == null || !(obj instanceof Empleadotieneventas))
            return false;

        Empleadotieneventas bean = (Empleadotieneventas) obj;

        if (this.vendedorcurp == null) {
            if (bean.vendedorcurp != null)
                return false;
        }
        else if (!this.vendedorcurp.equals(bean.vendedorcurp)) 
            return false;

        if (this.idventa != bean.idventa)
            return false;

        if (this.anio != bean.anio)
            return false;

        if (this.trimestre != bean.trimestre)
            return false;

        return true;
    }

    /* Creates and returns a copy of this object. */
    public Object clone()
    {
        Empleadotieneventas bean = new Empleadotieneventas();
        bean.vendedorcurp = this.vendedorcurp;
        bean.idventa = this.idventa;
        bean.anio = this.anio;
        bean.trimestre = this.trimestre;
        return bean;
    }

    /* Returns a string representation of the object. */
    public String toString() {
        String sep = "\r\n";
        StringBuffer sb = new StringBuffer();
        sb.append(this.getClass().getName()).append(sep);
        sb.append("[").append("vendedorcurp").append(" = ").append(vendedorcurp).append("]").append(sep);
        sb.append("[").append("idventa").append(" = ").append(idventa).append("]").append(sep);
        sb.append("[").append("anio").append(" = ").append(anio).append("]").append(sep);
        sb.append("[").append("trimestre").append(" = ").append(trimestre).append("]").append(sep);
        return sb.toString();
    }
}