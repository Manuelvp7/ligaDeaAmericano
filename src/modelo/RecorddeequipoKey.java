/*
 * This java source file is generated by DAO4J v1.19
 * Generated on Wed Mar 07 23:27:26 CST 2018
 * For more information, please contact b-i-d@163.com
 * Please check http://sourceforge.net/projects/dao4j/ for the latest version.
 */

package modelo;

public class RecorddeequipoKey implements java.io.Serializable, Cloneable {
    /* temporada */
    protected String temporada;

    /* nombreEquipo */
    protected String nombreequipo;
    
    public RecorddeequipoKey(){
        
    }

    public RecorddeequipoKey(String temporada, String equipo) {
        this.temporada = temporada;
        this.nombreequipo = equipo;
    }

    /* temporada */
    public String getTemporada() {
        return temporada;
    }

    /* temporada */
    public void setTemporada(String temporada) {
        this.temporada = temporada;
    }

    /* nombreEquipo */
    public String getNombreequipo() {
        return nombreequipo;
    }

    /* nombreEquipo */
    public void setNombreequipo(String nombreequipo) {
        this.nombreequipo = nombreequipo;
    }

    /* Calculate hash code */
    public int hashCode() {
        int hashCode = 0;
        if (temporada != null)  
            hashCode += temporada.hashCode();
        if (nombreequipo != null)  
            hashCode += nombreequipo.hashCode();
        return hashCode;
    }
    
    /* Indicates whether some other object is "equal to" this one. */
    public boolean equals(Object obj) {
        if (this == obj)
            return true;

        if (obj == null || !(obj instanceof RecorddeequipoKey))
            return false;

        RecorddeequipoKey key = (RecorddeequipoKey) obj;

        if (this.temporada == null) {
            if (key.temporada != null)
                return false;
        }
        else if (!this.temporada.equals(key.temporada)) 
            return false;

        if (this.nombreequipo == null) {
            if (key.nombreequipo != null)
                return false;
        }
        else if (!this.nombreequipo.equals(key.nombreequipo)) 
            return false;

        return true;
    }

    /* Creates and returns a copy of this object. */
    public Object clone()
    {
        RecorddeequipoKey key = new RecorddeequipoKey();
        key.temporada = this.temporada;
        key.nombreequipo = this.nombreequipo;
        return key;
    }

    /* Returns a string representation of the object. */
    public String toString() {
        String sep = "\r\n";
        StringBuffer sb = new StringBuffer();
        sb.append(this.getClass().getName()).append(sep);
        sb.append("[").append("temporada").append(" = ").append(temporada).append("]");
        sb.append("[").append("nombreequipo").append(" = ").append(nombreequipo).append("]");
        return sb.toString();
    }
}